<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.czl.goods.dao.ISubitemDAO">

	<select id="findAllByItem" parameterType="long" resultType="Subitem">
		SELECT sid,iid,title FROM subitem WHERE iid=#{iid}
	</select>
	

</mapper>